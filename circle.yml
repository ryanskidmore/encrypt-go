dependencies:
  override:
    - mkdir -p "${GOPATH}/src/go.pedge.io/encrypt"
    - rsync -azC --delete ./ "${GOPATH}/src/go.pedge.io/encrypt/"
    - make -C "${GOPATH}/src/go.pedge.io/encrypt" testdeps
test:
  override:
    - make -C "${GOPATH}/src/go.pedge.io/encrypt" test
